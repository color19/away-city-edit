# away · 城市精选

从华盛顿 IAD 出发，按「值得度」挑选下一个目的地：真实航班报价 × 城市体验 × 出发季节。

- 打开方式：直接用浏览器打开 `index.html`，无需安装或服务器。
- 价格：来自 Google Flights 探索页的快照（采集日期见页面），并非实时可订价；目前仅覆盖欧洲 28 个城市，缺失的城市不会用估算价补齐。
- 体验评分：编辑主观打分（美食、文化、自然、氛围），见 `data/profiles.js`，可自行修改。
- 值得度 = 体验分 × 季节系数 × 价格系数（0.8–1.2）。

数据生成脚本在 `scripts/`。
